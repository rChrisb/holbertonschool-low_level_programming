#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: 'Using the function printf
 * instead of the function puts'
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
