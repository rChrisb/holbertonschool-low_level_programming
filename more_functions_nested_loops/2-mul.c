#include "main.h"
#include <stdio.h>

/**
 * mul - multiplication of two integers and return the result's value
 * @a: first parameter
 * @b: second parameter
 * Return: return the addition
 *
 */

int mul(int a, int b)
{
	return (a * b);
}
