#include "main.h"
#include <stdio.h>

/**
 * add - adds two integers and return value
 * @n: first parameter
 * @c: second parameter
 * Return: return the addition
 *
 */

int add(int n, int c)
{
	int r = n + c;
	return (r);
}
